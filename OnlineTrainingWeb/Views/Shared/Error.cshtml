@model System.Web.Mvc.HandleErrorInfo

@{
    ViewBag.Title = "Error";
    Layout = "~/Views/Shared/_LoingLayout.cshtml";
}


<div class="LoginMainBackground">
    <div class="container">

        <div class="row">

            <div class="col-md-7">
                <div class="card card-body my-5 py-5 bg-transparent" id="WhoIsThisProgramFor">
                    @{
                        if (String.IsNullOrEmpty(ViewBag.errorMessage))
                        {
                            <h2 class="text-danger">An error occurred while processing your request.</h2>
                        }
                        else
                        {
                            <div class="card-title">
                                <h4 class="text-danger"><i class="fas fa-envelope"></i> @ViewBag.Message</h4>
                                <h5 class="text-white-50">@ViewBag.UserName</h5>
                                <h5 class="text-white-50">@ViewBag.errorMessage</h5>
                            </div>
                            <hr class="hr-light" />
                            <div class="flex-row">
                                <a class="card-link text-white-50">Your confirmation link will expire in 6 hours</a>

                            </div>

                        }
                    }

                </div>
                <!-- Card -->
               

            </div>
            <div class="col-md-5">
                <lottie-player src="https://assets6.lottiefiles.com/packages/lf20_lcjjitzn.json" background="transparent" speed="1" style="width: auto; height: auto;" loop autoplay></lottie-player>
            </div>

        </div>

    </div>
</div>



@*@{
    if (String.IsNullOrEmpty(ViewBag.errorMessage))
    {
        <h2 class="text-danger">An error occurred while processing your request.</h2>
    }
    else
    {
        <h2 class="text-danger">@ViewBag.errorMessage</h2>
    }
}*@

